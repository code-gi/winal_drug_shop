import 'package:flutter/material.dart';

// Define your constant colors
const Color primaryBlue = Colors.blue;
const Color primaryWhite = Colors.white;

// Optional: Create a class to organize your theme colors
class AppColors {
  static const Color blue = primaryBlue;
  static const Color white = primaryWhite;
}